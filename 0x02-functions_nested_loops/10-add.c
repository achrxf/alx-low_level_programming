#include "main.h"
/**
 * add - A function that adds two integers and returns the result.
 * @i: int type number
 * @j: int type number
 * Return: Always 0 (success).
*/

int add(int i, int j)
{
	int sum = i + j;

	return (sum);
}
