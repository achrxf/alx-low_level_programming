#include <stdio.h>

/**
 * main - Prints numbers from 0 to 10
 * Return: Always 0 (success)
*/
int main(void)
{
	int i = 48;

	while (i <= 57)
	{
		putchar(i);
		i++;
	}
	putchar('\n');

	return (0);
}
